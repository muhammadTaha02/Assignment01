import 'dart:ffi';
import 'dart:math';

void main() {
  // Map <String,String> info = {"1": "Taha", "2": "hamza", "3": "Bilal", "4": "Rehman"};
  // var Keyswithlength4 = info.keys.where((key) => key.length == 4);

  // print('Keys with length 4 = $Keyswithlength4');
  // Map<String, String> info = {
  //   'name': 'Alice',
  //   'phone': '1234',
  //   'city': 'Lahore',
  //   'code': '5678'
  // };

  // // Finding all keys with length 4
  // var keysWithLength4 = info.keys.where((key) => key.length == 4);

  // // Printing the keys with length 4
  // print('Keys with length 4: $keysWithLength4');

// var num = max(a, b)

var num = max(a, b)









}
